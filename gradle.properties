# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache
org.gradle.configuration-cache=false

# Fabric Properties
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.17.2
loom_version=1.11-SNAPSHOT

# Mod Properties
mod_version=1.0.0
maven_group=net.cozystudios.theultimatecompatmod
archives_base_name=theultimatecompatmod

# Dependencies
fabric_version=0.92.6+1.20.1

# JEI (compile-only reference; runtime is optional)
jei_version=15.21.0.148
